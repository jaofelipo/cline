export interface Locale {
    McpConfigurationView:{
        headerTitle: string
        doneButton: string
        tabs: {
          marketplace: string
          remoteServers: string
          installed: string
        }
    }
}

